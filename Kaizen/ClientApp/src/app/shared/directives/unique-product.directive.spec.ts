import { UniqueProductDirective } from './unique-product.directive';

describe('UniqueProductDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueProductDirective();
    expect(directive).toBeTruthy();
  });
});
