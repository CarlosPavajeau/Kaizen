import { PeriodicityPipe } from './periodicity.pipe';

describe('PeriodicityPipe', () => {
  it('create an instance', () => {
    const pipe = new PeriodicityPipe();
    expect(pipe).toBeTruthy();
  });
});
