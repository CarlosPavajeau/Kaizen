import { ClientsFilterPipe } from './clients-filter.pipe';

describe('ClientsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
