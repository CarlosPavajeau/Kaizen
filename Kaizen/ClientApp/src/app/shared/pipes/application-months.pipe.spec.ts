import { ApplicationMonthsPipe } from './application-months.pipe';

describe('ApplicationMonthsPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationMonthsPipe();
    expect(pipe).toBeTruthy();
  });
});
