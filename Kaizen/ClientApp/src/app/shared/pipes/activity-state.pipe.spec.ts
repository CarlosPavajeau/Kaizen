import { ActivityStatePipe } from './activity-state.pipe';

describe('ActivityStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ActivityStatePipe();
    expect(pipe).toBeTruthy();
  });
});
