import { FilterEquipmentsPipe } from './filter-equipments.pipe';

describe('FilterEquipmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEquipmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
